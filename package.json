{
  "name": "vite",
  "description": "Include Bootstrap's source Sass and individual JavaScript plugins with Vite.",
  "version": "0.0.0",
  "homepage": "https://glucoseguardian.it",
  "stackblitz": {
    "startCommand": "npm start"
  },
  "scripts": {
    "start": "vite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build",
    "predeploy": "npm run build && echo glucoseguardian.it > src/dist/CNAME",
    "deploy": "gh-pages -d src/dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "sass": "^1.54.8",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.0",
    "bootstrap-icons": "^1.10.3",
    "chart.js": "^2.9.0"
  }
}
